<% content_for :breadcrumb do %>
  <div class="ui basic segment breadcrumb">
    <%= link_to '课程列表', admin_courses_path, class: 'section' %>
    <i class="fas fa-angle-right divider"></i>
    <div class="section"><%= @course_crowd.course.title %></div>
    <i class="fas fa-angle-right divider"></i>
    <div class="section"><%= @course_crowd.crowd.name %></div>
    <i class="fas fa-angle-right divider"></i>
    <div class="section">排课</div>
  </div>
<% end %>

<%= render 'menu' %>
<%= render partial: 'booking/time_plans/index', locals: { plan: @course_crowd } %>
