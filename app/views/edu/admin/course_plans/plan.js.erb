$('#modal').replaceWith('<%= j(render 'plan') %>');
$('#modal').modal({
  onHide: function(element) {
    Turbolinks.visit(window.location);
  }
}).modal('show');
remote_js_load(<%= raw remote_js_load %>)
