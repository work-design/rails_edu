<%= form_with theme: 'search', model: Exam.new do |f| %>
  <div class="fields">
    <%= f.collection_select :member_id, @members, :id, :name, prompt: 'Search Member' %>
    <%= f.submit %>
  </div>
<% end %>


