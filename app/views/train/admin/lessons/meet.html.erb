<%= render 'nav_lesson' %>

<div class="ui basic segment breadcrumb">
  <%= link_to 'Back', edu_lessons_path, class: 'section' %>
  <div class="divider"> / </div>
  <%= link_to 'Detail', edu_lesson_path(@lesson), class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Edit Meet</div>
</div>

<div class="ui segment">

  <%= form_with model: @lesson, scope: :lesson, url: edu_lesson_path(@lesson), method: 'patch', on: { wrapper_id: true } do |f| %>
    <%= render partial: 'rails_booking/time/form', locals: { f: f } %>
  <% end %>

</div>
